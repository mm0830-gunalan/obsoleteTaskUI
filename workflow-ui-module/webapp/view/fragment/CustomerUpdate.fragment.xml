<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="customerDialog"
        title="Update Customer Response"
        draggable="true"
        resizable="true"
    >
        <VBox class="sapUiSmallMargin">
            <Label text="Customer Response" />
            <Select
                width="100%"
                selectedKey="{customer>/customerResponse}"
                forceSelection="false"
            >
                <items>
                    <core:Item
                        key="LAPP checks internal usage"
                        text="LAPP checks internal usage"
                    />
                    <core:Item
                        key="Customer pays for scrapping"
                        text="Customer pays for scrapping"
                    />
                    <core:Item
                        key="Customer purchases obsolete stock"
                        text="Customer purchases obsolete stock"
                    />
                </items>
            </Select>

            <!-- New Comment Field -->
            <Label
                text="Comment"
                class="sapUiSmallMarginTop"
            />
            <TextArea
                value="{customer>/commentCustomer}"
                width="100%"
                rows="4"
                placeholder="Enter comment..."
            />
        </VBox>

        <buttons>
            <Button
                text="Cancel"
                press="onCancelCustomerDialog"
            />

            <Button
                text="Apply"
                type="Emphasized"
                press="onApplyCustomerUpdate"
            />
        </buttons>
    </Dialog>
</core:FragmentDefinition>
